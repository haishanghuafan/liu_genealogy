services:
  web:
    build: .
    ports:
      - "8010:8010"
    volumes:
      - .:/app
      - ./media:/app/media
      - ./staticfiles:/app/staticfiles
    env_file:
      - .env
    restart: unless-stopped
